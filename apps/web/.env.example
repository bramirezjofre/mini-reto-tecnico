# Frontend NextJS
# Server-only URL of the NestJS backend (NOT exposed to the browser bundle).
API_URL=http://localhost:3001
# Public GitHub username shown on the page.
NEXT_PUBLIC_GITHUB_USERNAME=bramirezjofre
